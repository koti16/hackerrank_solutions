#include<iostream>
using namespace std;

int main (){
    int a,b,c;
    cout<<"enter a ,b,c:";
    cin>> a;
    cin>>b;
    cin>>c;
    if(a>b && a>c){
        cout<<"A is largest number.";
    }
    else if ( b>a && b>c){
        cout<<"B is largest number";
        
    }
    else 
    {
        cout<<"C is largest number";
    }
    return 0;
}
