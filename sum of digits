#include<iostream>
using namespace std;

int main()
{
    int n, digit, sum = 0;

    cout << "Enter a number:" << endl;
    cin >> n;

    while(n)
    {
        digit = n % 10;
        sum += digit;
        n /= 10;
    }

    cout << sum << endl;

    return 0;
}
